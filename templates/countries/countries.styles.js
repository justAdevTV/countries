import styled from "styled-components";

const _Countries = styled.section`
  display: grid;
`;

const _Navbar = styled.div``;

const _SearchBar = styled.div``;

const _SelectBox = styled.div``;

const _CountryCards = styled.div``;

export { _Countries, _Navbar, _SearchBar, _SelectBox, _CountryCards };
